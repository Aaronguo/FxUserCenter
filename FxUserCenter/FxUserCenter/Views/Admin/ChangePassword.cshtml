@{
    ViewBag.Title = "修改密码";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div>
    <ul class="breadcrumb">
        <li>
            <a href="#">用户中心</a> <span class="divider">/</span>
        </li>
        <li>
            <a href="#">修改密码</a>
        </li>
    </ul>
</div>


@using (Html.BeginForm("ChangePassword", "Admin", FormMethod.Post, new { enctype = "multipart/form-data", id = "transfercarform" }))
{
    <div class="row-fluid sortable">
        <div class="box span12">
            <div class="box-header well" data-original-title="">
                <h2><i class="icon-edit"></i>修改密码</h2>
                <div class="box-icon">
                    <a href="#" class="btn btn-setting btn-round"><i class="icon-cog"></i></a>
                    <a href="#" class="btn btn-minimize btn-round"><i class="icon-chevron-up"></i></a>
                    <a href="#" class="btn btn-close btn-round"><i class="icon-remove"></i></a>
                </div>
            </div>
            <div class="box-content">
                <form class="form-horizontal">
                <fieldset>
                    <div class="control-group success">
                        <label class="control-label" for="inputSuccess">请输入原始密码</label>
                        <div class="controls">
                            @Html.TextBox("oldPassword")
                            <span class="help-inline">@ViewBag.Old</span>
                        </div>
                    </div>
                    <div class="control-group success">
                        <label class="control-label" for="inputSuccess">请输入新密码</label>
                        <div class="controls">
                             @Html.TextBox("newPassword")
                        </div>
                    </div>
                    <div class="control-group success">
                        <div class="controls">
                            <span class="help-inline">@ViewBag.New</span>
                        </div>
                    </div>
                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">保存</button>
                    </div>
                </fieldset>
                </form>
            </div>
        </div>
    </div>
}
